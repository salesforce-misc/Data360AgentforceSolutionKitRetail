/**
 * Enable Account as Buyer
 */

String accountName = 'Mark Smith';      // Change this to your account name
String storeName = 'DIYStorefront';     // Change this to your store name

SecurityConfigHelper.enableAccountAsBuyer(accountName, storeName);
